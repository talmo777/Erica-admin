# 작업 내역 — ERICA 공모전 일괄 수집(수집 인박스)

> 이번 작업에서 **무엇을 의도했고, 무엇을 구현했는지** 정리한 문서입니다.
> (코드 외에 의사결정 배경·실행법·남은 일 포함 — 인수인계용)

---

## 1. 목표 / 의도

이 프로그램의 최종 목적은 **ERICA 공모전 소식을 한곳에 모아 분류·시각화·연동**하는 것입니다.
이번 작업은 그 파이프라인의 **첫 단계 = "여러 사이트의 공지를 목록째 긁어와 한 곳에 쌓는 일괄 수집(bulk ingestion)"** 을 0에서 작동 상태까지 구축한 것입니다.

설계 의도:
- **원본 그대로 수집**(분류는 나중 단계). 본문·포스터 이미지 URL·첨부파일 URL을 **함께 저장**해서, 나중에 OCR/문서파싱으로 분류할 때 **재크롤 없이** 쓰도록.
- **소스를 쉽게 늘릴 수 있는 구조**(어댑터 패턴) — ERICA는 단과대마다 게시판 솔루션이 제각각이라 확장성이 핵심.
- **중복 수집 방지** — 같은 날짜·소스를 또 긁지 않도록 UI에서 수집 현황을 보여줌.

---

## 2. 아키텍처 결정

| 결정 | 이유 |
|---|---|
| 크롤러를 **standalone Node 서버**로 분리 (관리자 React 앱과 별개) | 앱은 클라이언트 SPA라 cheerio/Playwright 같은 서버사이드 크롤링 불가. 크롤러는 로컬 HTTP 서버(8787)로 두고 UI가 호출. |
| **소스 어댑터 레지스트리** (`crawler/src/sources/`) | 사이트마다 구조가 달라 어댑터 1개씩 추가. 같은 플랫폼은 **팩토리로 공용화**. |
| 저장은 **로컬 JSONL**(`crawler/out/inbox.jsonl`) | 외부 의존 없이 즉시 동작. Supabase는 dual-write로 추가(준비됨). |
| 기술 스택 **Node/cheerio 유지** | 기존 repo가 JS/TS라 통합 용이 (Python 대신). |

데이터 흐름:
```
[여러 ERICA 공지 사이트] → 크롤러 어댑터(cheerio/Playwright) → InboxStore(JSONL 누적)
                                                          ↘ (준비됨) Supabase raw_notices
관리자 앱 "수집 인박스" 탭 ──HTTP──> 크롤러 서버(:8787) /api/*
```

---

## 3. 구현 — 크롤러 (소스 11개)

처음엔 전체공지 1곳뿐 → **11개 소스**로 확장. 사이트별 기술방식이 다릅니다:

| 소스 | 사이트 | 기술 방식 |
|---|---|---|
| 전체공지 (notice_all) | hanyang.ac.kr/web/www/notice_all | Liferay NoticeBoardPortlet, 목록 날짜·캠퍼스 badge |
| 창업교육센터 (eec) | eec.hanyang.ac.kr | 자체 게시판(?mode=view&No=), 날짜는 상세에서 |
| SW융합대학 (computing) | computing.hanyang.ac.kr | PHP 게시판(/open/notice.php?idx=) |
| 경상대학 (ibus) | ibus.hanyang.ac.kr | 자체 CMS(/front/community/notice/view?id=) |
| 약학대학 (pharmacy) | pharmacy.hanyang.ac.kr | 〃 (ibus와 같은 CMS, 팩토리 공용) |
| 공학대학 (ieng) | ieng.hanyang.ac.kr | 한양 공용 Liferay BbsPortlet (onclick viewMessage) |
| 언론정보대학 (comm) | comm.hanyang.ac.kr | 〃 |
| 글로벌콘텐츠융합 (gcc) | gcc.hanyang.ac.kr | 〃 |
| 비교과통합 (hylu_e) | hylu-e.hanyang.ac.kr | 프로그램 그리드 + **AJAX 페이지네이션**(/ko/program/all/list/0/{page}), 커버 포스터 |
| LIONS칼리지 (lions) | lions.hanyang.ac.kr | **ASP.NET 포스트백 → Playwright**(헤드리스 브라우저)로 상세 수집 |
| 디자인대학 (idesign) | idesign.hanyang.ac.kr | gnuboard(/bbs/board.php?wr_id=) |

공통 구현:
- **본문·포스터 이미지 URL·첨부(hwpx/pdf) URL** 추출 + 게시일·부서·캠퍼스·카테고리
- **날짜 범위 수집**(from~to) + **캠퍼스 필터**(기본 `ERICA + 한양(공통)`)
- **중복 제거**(post_id 기준 upsert)
- 까다로운 케이스 정복:
  - **TLS 차단 사이트**(computing 등): 인증서 검증 우회로 접속
  - **디자인대학**: `design.hanyang.ac.kr`은 WAF 차단이었는데, **실제 주소가 `idesign`** 임을 정찰로 발견 → gnuboard로 해결
  - **LIONS**: 해시가 매 로드마다 바뀌어 → 행 위치(index) 클릭 + 제목·날짜 기반 안정 키로 처리
- **현재 232건 로컬 저장**(데이터 안전)

핵심 파일:
```
crawler/
  src/
    sources/        # 어댑터들
      index.ts        - 소스 레지스트리
      types.ts        - SourceAdapter 인터페이스
      util.ts         - runListBoard(목록 게시판 공용 러너) + 상세파싱 유틸
      notice_all.ts, eec.ts, computing.ts, hylu_e.ts, lions.ts, idesign.ts
      liferay_college.ts  - 공학/언론정보/글로벌콘텐츠 (Liferay 공용 팩토리)
      front_cms.ts        - 경상/약학 (자체 CMS 공용 팩토리)
    parse.ts        - notice_all 파서
    crawl.ts        - crawlNotices (from/to 날짜 필터)
    store.ts        - InboxStore (JSONL 누적, status 관리)
    runsStore.ts    - 수집 실행 이력
    fetchPage.ts    - fetch(재시도+TLS우회)
    browser.ts      - Playwright 싱글톤(LIONS용)
    supabaseSink.ts - Supabase 적재(준비됨)
  server.ts         - 로컬 HTTP API (포트 8787)
  run.ts            - CLI 크롤
  backfill.ts       - 기존 JSONL → Supabase 백필
  sql/raw_notices.sql - DB 테이블 정의
  out/              - 수집 데이터(JSONL)
```

서버 API:
```
GET  /api/health                          상태
GET  /api/sources                         등록 소스 목록
GET  /api/notices                         수집 항목 전체
GET  /api/runs                            수집 실행 이력
POST /api/crawl   {sources,from,to,campus,detail}   크롤 실행
POST /api/notices/status {post_id,status}           상태 변경
```

---

## 4. 구현 — 관리자 UI ("수집 인박스" 새 탭)

`pages/CrawlInbox.tsx` (+ `services/crawlerApi.ts`, 라우트 `/admin/inbox`, 사이드바 메뉴):

- **소스 선택 칩** — 어느 사이트를 긁을지 토글
- **날짜 범위** — from~to 직접 입력 + 빠른버튼(오늘/최근10일/최근20일/전체) + 캠퍼스 선택
- **필터** — 출처·카테고리·상태·제목 검색
- **통계 카드** — 총 수집/신규/오늘 게시
- **상세 모달** — 카드 클릭 시 본문 전체·이미지(포스터)·첨부 링크 표시
- **수집 달력** — 날짜별 수집 건수 시각화(월 이동), "안 긁은 날" 구분
- **수집 이력 + 중복 범위 경고** — 이미 긁은 범위를 또 고르면 경고 → 중복 수집 방지
- **상태 관리** — 공모전 후보 / 검토함 / 무시

---

## 5. 실행 방법

서버 2개를 띄웁니다:
```bash
# 1) 크롤러 서버 (포트 8787)
cd crawler
npm install
npm run serve

# 2) 관리자 앱 (포트 5173)
cd ..
npm install
npm run dev
# → http://localhost:5173/admin/inbox
```
환경변수: `.env.example`를 복사해 `.env.local` 만들고 Supabase 키 입력
(Supabase 키가 없어도 크롤러/수집 인박스 자체는 동작).

---

## 6. Supabase DB화 — 코드 준비 완료, 1단계 대기

JSONL → Supabase `raw_notices` 적재 코드(dual-write + 백필)는 **완성**돼 있습니다.
**남은 단 한 단계: 테이블 생성.** anon 키로는 DDL이 안 되므로, Supabase 대시보드 SQL Editor에서 `crawler/sql/raw_notices.sql`을 1회 실행하면 됩니다.
이후 `cd crawler && npm run backfill`로 기존 데이터를 올리고, 이후 크롤은 자동으로 DB에도 적재됩니다.

---

## 7. 전략적 발견 (의사결정 근거)

- **전체공지는 "본부 중심"** 채널이라 단과대 알짜 공모전은 따로 긁어야 함을 데이터로 확인 → 단과대 확장의 근거.
- **디자인대학 차단은 "주소가 틀렸던 것"**(design≠idesign)이지 못 긁는 게 아니었음 → 정찰로 해결.
- **HY-in 포털**(로그인+OTP)은 공모전 수율 낮고 난이도 최상이라 **합리적으로 보류**.
- 분류엔 **원문/사진/파일 URL이 "재료"로는 충분**하지만, 실제 분류엔 **포스터/첨부 내용 추출(OCR/문서파싱)**이 추가로 필요 (정보가 이미지·첨부 안에 있는 공지가 많음).

---

## 8. 남은 일 (다음 단계)

1. **Supabase 테이블 생성 → DB 동기화** (코드 준비됨, 1단계만)
2. 분류용 **이미지 OCR / PDF·HWPX 텍스트 추출** ← 분류의 핵심 병목
3. 공모전 **구조화**(마감일·주최·카테고리 추출) → **중복 제거** → 기존 `공모전 게시/배포`(contests) 연동
4. (선택) 자동 스케줄링(매일 크롤), 사업단(SW중심대학/LINC) 등 소스 추가

---

---

_요약: "여러 ERICA 사이트의 공모전 공지를 본문·포스터·첨부까지, 날짜별로 한 곳에 모아 보고 관리하는 시스템"을 0에서 작동 상태까지 완성. 다음은 모은 데이터를 분류·연동하는 단계._
